# Core (generator + benchmark) — only the stdlib is needed beyond these two
requests>=2.28,<3
beautifulsoup4>=4.11,<5

# Benchmark tool — install at least one AI SDK:
anthropic>=0.39,<1     # pip install anthropic  (set ANTHROPIC_API_KEY)
openai>=1.0,<2         # pip install openai     (set OPENAI_API_KEY)
# Gemini uses REST via requests — no extra SDK needed (set GOOGLE_API_KEY)
